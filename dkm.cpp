#include <bits/stdc++.h>
using namespace std;

int main(){
    vector<vector<int>> ss;
    vector<int> b;
    b.push_back(1);
    b.push_back(2);
    vector<int> tmp(b.rbegin(), b.rend());
    ss.insert(tmp);
}